version: "3.8"

services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1

  ui:
    build: .
    ports:
      - "8501:8501"
    command: ["python", "-m", "streamlit", "run", "turnstile/app.py", "--server.port=8501", "--server.address=0.0.0.0"]
